# Install with CasaOS

Use o compose em [deploy/casaos/docker-compose.yml](/home/matheus/torrentio-torznab-repo/deploy/casaos/docker-compose.yml).

Ajuste pelo menos:

- `TORZNAB_BASE_URL`
- `TORZNAB_SOURCES`
- volume persistente em `/config`
